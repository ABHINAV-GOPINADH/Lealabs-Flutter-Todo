import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF1F1F1F);
  static const card = Color(0xFF2A2A2A);
  static const accent = Colors.teal;
  static const delete = Colors.redAccent;
  static const text = Colors.white;
  static const hint = Colors.white54;
  static const icon = Colors.tealAccent;
}
